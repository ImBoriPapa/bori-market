server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ImBoriPapa/bori-market-colud-cofig.git
#          username:
#          password:

