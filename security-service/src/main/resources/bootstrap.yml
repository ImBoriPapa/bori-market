spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: bori-market
  profiles:
    active: test
#  config:
#    activate:
#      on-profile: "test"
---
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: bori-market
  profiles:
    active: prod
#  config:
#    activate:
#      on-profile: "prod"